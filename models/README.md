# Models

This folder is reserved for future machine learning experiments.

## Possible Future Workflow

1. Train a model in Python / PyTorch.
2. Export the model to ONNX or TensorFlow.js.
3. Load the model directly in the browser.
4. Compare model output against random and rule-based baselines.

> **Note:** Do not add large model files to this directory unless they are optimized and small enough for GitHub Pages deployment.
